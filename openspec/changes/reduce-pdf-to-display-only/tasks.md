## 1. 阅读态回退

- [x] 1.1 将 `PdfViewerDialog` 收敛为 display-only 布局，移除批注侧栏、保存/重载状态提示、删除入口和残留批注文案。
- [x] 1.2 简化 `PdfJsViewer`，保留 PDF.js 加载、翻页、缩放和打开原文件能力，移除标注工具、文本选区创建、矩形拖拽和批注层交互。
- [x] 1.3 确保文档内嵌 PDF 继续使用当前轻量预览和打开流程，不新增任何批注显示或编辑行为。

## 2. 状态与类型收缩

- [x] 2.1 从 PDF 显示主路径中移除 `attachments.pdfState.get/update` 调用、`pdfAttachmentState` 相关前端状态和冲突处理。
- [x] 2.2 清理前端共享类型、帮助函数和测试中仅为用户可见 PDF 批注能力服务的活跃依赖，确保显示模式不再引用它们。
- [x] 2.3 明确 `AttachmentPdfState` 模型、路由和历史数据的保留策略，并确保当前 PDF 阅读流程完全不依赖这些后端能力。

## 3. 验证

- [x] 3.1 更新 PDF viewer 相关测试，覆盖 display-only 阅读态并断言不显示任何批注工具、列表、保存按钮或批注状态消息。
- [x] 3.2 更新或删除与前端 PDF 批注读写直接相关的测试，确保显示路径不再触发 `pdfState` 请求。
- [x] 3.3 运行与 PDF 显示相关的测试、类型检查和构建验证，确认 PDF.js worker、阅读控制和原文件打开流程仍可用。
